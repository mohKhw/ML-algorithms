import numpy as np

def cross_correlation(image, filter, mode='valid'):
    
    return 111
f[-1,-1]*I[5+(-1),1+(-1)] + f[-1,0]*I[5+(-1),1+0] + f[0,-1]*I[5+0,1+(-1)] + f[0,0]*I[5+0,1+0] + f[1,0]*I[5+1,1+0] + f[0,1]*I[5+0,1+1] + f[1,1]*I[5+1,1+1]
def convolution(image, filter, mode='valid'):
    return 110
